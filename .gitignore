# Python virtual environments
.venv/
venv/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Transcripts and documents (generated)
*.txt
*.docx

# Keep important text files (don't ignore these even though we ignore *.txt above)
!requirements.txt
!README.md

# Audio / Video (generated or large files)
*.mp3
*.wav
*.flac
*.m4a
*.aac
*.ogg
*.mp4
*.mov
*.avi
*.mkv

# FFmpeg binaries and wrappers
# We commonly ignore generated binaries, but if a bundled ffmpeg is included in the repo
# it should be tracked. Negate any broader ignore here so `ffmpeg.exe`/`ffmpeg.bat` can be committed.
!ffmpeg.exe
!ffmpeg.bat

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
